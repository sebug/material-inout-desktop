# Material In/Out, desktop edition
Making a version that'll work without Docker etc.

## Installation Steps
To run the program locally, you have to install the workload:

   sudo dotnet workload install maui
